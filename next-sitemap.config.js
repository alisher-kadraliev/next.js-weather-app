// next-sitemap.config.js

/** @type {import('next-sitemap').IConfig} */
const config = {
    siteUrl: 'https://next-js-weather-app-seven.vercel.app', // Replace with your site's URL
    generateRobotsTxt: true, // (optional) Enable to generate robots.txt file
};

module.exports = config;
